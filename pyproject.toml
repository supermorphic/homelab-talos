[project]
name = "homelab-talos-test-tools"
version = "0.1.0"
requires-python = "==3.13.*"
dependencies = [
  "PyYAML==6.0.3",
]

[dependency-groups]
dev = [
  "ruff==0.16.0",
]

[tool.uv]
package = false
python-preference = "only-system"

[tool.ruff]
line-length = 99
target-version = "py313"
