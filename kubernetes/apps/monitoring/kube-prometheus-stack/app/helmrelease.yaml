---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kube-prometheus-stack
  namespace: monitoring
spec:
  chart:
    spec:
      chart: kube-prometheus-stack
      interval: 15m
      sourceRef:
        kind: HelmRepository
        name: prometheus-community
      version: 87.19.0
  # Retain the historical drift-detection setting while validating a bounded
  # values change. The July upgrade failure's root cause remains unproven;
  # see docs/specs/005-flux-reconciliation-alerting.md for current evidence.
  driftDetection:
    mode: disabled
  install:
    crds: Create
    remediation:
      retries: 3
  interval: 1h
  releaseName: kube-prometheus-stack
  targetNamespace: monitoring
  timeout: 20m
  upgrade:
    cleanupOnFail: true
    crds: CreateReplace
    remediation:
      retries: 3
      strategy: rollback
  valuesFrom:
    - kind: ConfigMap
      name: kube-prometheus-stack-values
      valuesKey: values.yaml
