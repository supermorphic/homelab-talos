---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: portainer
  namespace: portainer
  annotations:
    external-dns.k8s.io/audience: internal
    gethomepage.dev/enabled: "true"
    gethomepage.dev/name: "Portainer"
    gethomepage.dev/description: "Read-only Kubernetes runtime visibility"
    gethomepage.dev/group: "Monitoring"
    gethomepage.dev/icon: "portainer-dark.svg"
    gethomepage.dev/href: "https://portainer.lab.supermorphic.com"
    gethomepage.dev/pod-selector: "app.kubernetes.io/name=portainer"
    # Live Kubernetes widget. The API key is injected into Homepage from its
    # per-service SOPS Secret. Default Kubernetes fields are intentionally used.
    gethomepage.dev/widget.type: "portainer"
    gethomepage.dev/widget.url: "https://portainer.lab.supermorphic.com"
    gethomepage.dev/widget.env: "1"
    gethomepage.dev/widget.kubernetes: "true"
    gethomepage.dev/widget.key: "{{HOMEPAGE_VAR_PORTAINER_API_KEY}}"
spec:
  hostnames:
    - portainer.lab.supermorphic.com
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: internal
      namespace: networking
      sectionName: https
  rules:
    - backendRefs:
        - group: ""
          kind: Service
          name: portainer
          port: 9000
          weight: 1
      matches:
        - path:
            type: PathPrefix
            value: /
