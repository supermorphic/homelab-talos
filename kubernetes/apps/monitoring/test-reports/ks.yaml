---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: test-reports
  namespace: flux-system
spec:
  dependsOn:
    - name: cilium
    - name: internal-gateway
    - name: kube-prometheus-stack
    - name: longhorn
  interval: 1h
  path: ./kubernetes/apps/monitoring/test-reports/app
  prune: true
  retryInterval: 1m
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  # Bootstrap and a sanitized publication are operator-run acceptance gates. The
  # activation PR changes this to false and adds the Gatus endpoint.
  suspend: true
  timeout: 10m
  wait: true
