# ntfy via the bjw-s app-template chart. Single replica, Recreate on a Longhorn RWO PVC
# (SQLite cache/auth DBs are single-writer). The official image has no USER directive and
# the static binary only writes /var/lib/ntfy, so it runs as a fixed non-root UID with the
# PVC made writable via fsGroup (no init-copy needed). server.yml is a read-only ConfigMap
# mount; declarative auth (users/ACLs/tokens) comes from the SOPS ntfy-secret through
# explicitly mapped env keys — never envFrom, so the Secret's auth.yml key (consumed by
# the alertmanager-ntfy adapter) cannot leak into this container's environment.
controllers:
  ntfy:
    type: deployment
    strategy: Recreate
    pod:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault
      annotations:
        # Roll the pod when server.yml or the auth Secret change. config-hash is
        # `git hash-object app/server.yml`; sops-hash is `git hash-object
        # app/secret.sops.yaml`, stamped by `just repo ntfy-identity`. Both CI-verified.
        # (Named sops-hash, not *-secret-*, so gitleaks does not flag the blob hash.)
        config-hash: "497a68f3b29883fca3815a20b5af3482407af133"
        sops-hash: "1bde80fa91807b861efc5df6397e0039e8900709"
    containers:
      app:
        image:
          repository: docker.io/binwiederhier/ntfy
          tag: v2.26.3
        args: ["serve"]
        env:
          TZ: America/Denver
          NTFY_AUTH_USERS:
            valueFrom:
              secretKeyRef:
                name: ntfy-secret
                key: NTFY_AUTH_USERS
          NTFY_AUTH_ACCESS:
            valueFrom:
              secretKeyRef:
                name: ntfy-secret
                key: NTFY_AUTH_ACCESS
          NTFY_AUTH_TOKENS:
            valueFrom:
              secretKeyRef:
                name: ntfy-secret
                key: NTFY_AUTH_TOKENS
        resources:
          requests:
            cpu: 25m
            memory: 64Mi
          limits:
            # Memory-only limit per repo convention.
            memory: 256Mi
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop: ["ALL"]
        probes:
          # /v1/health is unauthenticated and returns {"healthy":true}.
          readiness:
            enabled: true
            custom: true
            spec:
              httpGet:
                path: /v1/health
                port: 80
              periodSeconds: 10
              failureThreshold: 3
          liveness:
            enabled: true
            custom: true
            spec:
              httpGet:
                path: /v1/health
                port: 80
              periodSeconds: 30
              failureThreshold: 5
          startup:
            enabled: true
            custom: true
            spec:
              httpGet:
                path: /v1/health
                port: 80
              periodSeconds: 5
              failureThreshold: 30
service:
  app:
    controller: ntfy
    ports:
      http:
        port: 80
      metrics:
        port: 9090
persistence:
  # RWO Longhorn PVC (renders as PVC "ntfy") holding cache.db + auth.db.
  config:
    type: persistentVolumeClaim
    accessMode: ReadWriteOnce
    size: 2Gi
    storageClass: longhorn
    annotations:
      helm.sh/resource-policy: keep
    globalMounts:
      - path: /var/lib/ntfy
  # Read-only server.yml from the generated ntfy-config ConfigMap.
  server-config:
    type: configMap
    name: ntfy-config
    globalMounts:
      - path: /etc/ntfy
        readOnly: true
