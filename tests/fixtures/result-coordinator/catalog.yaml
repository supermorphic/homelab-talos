schema_version: 1
executions:
  ci:
    - validation.core
    - validation.observability
    - validation.automation
    - validation.ci-framework
  ci-core:
    - validation.core
  ci-observability:
    - validation.observability
  ci-automation:
    - validation.automation
  ci-framework:
    - validation.ci-framework
suites:
  - metadata: {id: validation.ci, source: validation, framework: just, suite: ci, tier: offline, target: repository, scenario: null, scope: repository, intent: regression, mutates_cluster: false, execution_owner: shared}
    confirmation: {type: none, variable: null, expected: null}
    runner: {command: "mise exec -- just ci", implementation: .justfile}
    native_results: {strategy: aggregate}
  - metadata: {id: validation.core, source: validation, framework: bash, suite: ci, tier: offline, target: core, scenario: null, scope: repository, intent: regression, mutates_cluster: false, execution_owner: shared}
    confirmation: {type: none, variable: null, expected: null}
    runner: {command: "mise exec -- just fixture-pass", implementation: tests/fixtures/result-coordinator/fake-just.sh}
    native_results: {strategy: wrapper-junit}
  - metadata: {id: validation.observability, source: validation, framework: bash, suite: ci, tier: offline, target: observability, scenario: null, scope: repository, intent: regression, mutates_cluster: false, execution_owner: shared}
    confirmation: {type: none, variable: null, expected: null}
    runner: {command: "mise exec -- just fixture-fail", implementation: tests/fixtures/result-coordinator/fake-just.sh}
    native_results: {strategy: wrapper-junit}
  - metadata: {id: validation.automation, source: validation, framework: bash, suite: ci, tier: offline, target: automation, scenario: null, scope: repository, intent: regression, mutates_cluster: false, execution_owner: shared}
    confirmation: {type: none, variable: null, expected: null}
    runner: {command: "mise exec -- just fixture-skipped", implementation: tests/fixtures/result-coordinator/fake-just.sh}
    native_results: {strategy: wrapper-junit}
  - metadata: {id: validation.ci-framework, source: validation, framework: bash, suite: ci, tier: offline, target: ci-framework, scenario: null, scope: repository, intent: regression, mutates_cluster: false, execution_owner: shared}
    confirmation: {type: none, variable: null, expected: null}
    runner: {command: "mise exec -- just fixture-skipped", implementation: tests/fixtures/result-coordinator/fake-just.sh}
    native_results: {strategy: wrapper-junit}
