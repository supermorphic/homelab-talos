schema_version: 1
executions:
  ci:
    - validation.fixture-pass
    - validation.fixture-fail
    - validation.fixture-skipped
suites:
  - metadata: {id: validation.ci, source: validation, framework: just, suite: ci, tier: offline, target: repository, scenario: null, scope: repository, intent: regression, mutates_cluster: false, execution_owner: shared}
    confirmation: {type: none, variable: null, expected: null}
    runner: {command: "mise exec -- just ci", implementation: .justfile}
    native_results: {strategy: aggregate}
  - metadata: {id: validation.fixture-pass, source: validation, framework: bash, suite: ci, tier: offline, target: fixture-pass, scenario: null, scope: repository, intent: regression, mutates_cluster: false, execution_owner: shared}
    confirmation: {type: none, variable: null, expected: null}
    runner: {command: "mise exec -- just fixture-pass", implementation: tests/fixtures/result-coordinator/fake-just.sh}
    native_results: {strategy: wrapper-junit}
  - metadata: {id: validation.fixture-fail, source: validation, framework: bash, suite: ci, tier: offline, target: fixture-fail, scenario: null, scope: repository, intent: regression, mutates_cluster: false, execution_owner: shared}
    confirmation: {type: none, variable: null, expected: null}
    runner: {command: "mise exec -- just fixture-fail", implementation: tests/fixtures/result-coordinator/fake-just.sh}
    native_results: {strategy: wrapper-junit}
  - metadata: {id: validation.fixture-skipped, source: validation, framework: bash, suite: ci, tier: offline, target: fixture-skipped, scenario: null, scope: repository, intent: regression, mutates_cluster: false, execution_owner: shared}
    confirmation: {type: none, variable: null, expected: null}
    runner: {command: "mise exec -- just fixture-skipped", implementation: tests/fixtures/result-coordinator/fake-just.sh}
    native_results: {strategy: wrapper-junit}
