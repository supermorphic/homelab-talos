# Chainsaw validation fixtures

Files below this directory exercise offline Chainsaw validation only. Live
runners discover scenarios exclusively below `tests/chainsaw/` and must never
execute these fixtures.
