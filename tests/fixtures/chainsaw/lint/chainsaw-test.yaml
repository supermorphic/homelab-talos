apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: offline-lint-contract
spec:
  steps:
    - name: validate-test-schema
      try:
        - assert:
            resource:
              apiVersion: v1
              kind: ConfigMap
              metadata:
                name: never-executed
