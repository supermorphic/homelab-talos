apiVersion: chainsaw.kyverno.io/v1alpha2
kind: Configuration
metadata:
  name: homelab-talos
spec:
  cleanup:
    skipDelete: false
  execution:
    failFast: true
    parallel: 1
  timeouts:
    apply: 1m
    assert: 2m
    cleanup: 1m
    delete: 1m
    error: 30s
    exec: 1m
